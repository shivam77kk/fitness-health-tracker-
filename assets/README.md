# Assets Directory

This directory contains assets for the FitPulse application.

## Logo

The application uses inline SVG icons in the HTML files. If you want to add a custom logo.png file, place it here and update the references in index.html and dashboard.html.
